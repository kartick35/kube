apiVersion: v1
kind: Service
metadata:
  name: {{ include "ndemoproj.fullname" . }}
  labels:
    {{- include "ndemoproj.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ndemoproj.selectorLabels" . | nindent 4 }}
